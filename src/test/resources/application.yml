spring:
  datasource:
    url: jdbc:h2:mem:uidai_mom;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false

external:
  user-service:
    enabled: false
    base-url: http://localhost:9001
  activity-service:
    enabled: false
    base-url: http://localhost:9002
